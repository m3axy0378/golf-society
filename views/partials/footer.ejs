  </main>
  <% if (typeof hideTagline === 'undefined' || !hideTagline) { %>
    <footer class="site-footer">
      <span class="logo-wordmark">Tee <span class="accent">League</span></span>
      &middot; Play anywhere. <span class="accent">Compete</span> everywhere.
    </footer>
  <% } %>
  <% if (currentPlayer) { %>
    <script src="/offline-queue.js" defer></script>
  <% } %>
</body>
</html>
